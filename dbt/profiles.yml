de_study_dbt_profile: # dbt_project.yml에서 지정한 이름
  target: dev
  outputs:
    dev:
      type: postgres
      host: postgres         # 🌟 docker-compose.yml에 정의된 서비스 이름
      port: 5432
      user: airflow
      pass: airflow
      dbname: analytics_db   # 🌟 1단계에서 생성한 데이터 웨어하우스용 DB
      # schema: raw          # dbt가 기본으로 사용할 스키마
      schema: public      # dbt가 기본으로 사용할 스키마